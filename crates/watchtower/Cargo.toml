[package]
name = "watchtower"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
engine = { path = "../engine" }
prover = { path = "../prover" }
chain = { path = "../chain" }
serde = { version = "1.0", features = ["derive"] }
