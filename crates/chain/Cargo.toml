[package]
name = "chain"
version = "0.1.0"
edition = "2021"

[dependencies]
prover = { path = "../prover" }
common = { path = "../common" }
engine = { path = "../engine" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
