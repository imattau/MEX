[package]
name = "validation"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
ed25519-dalek = { version = "2.1", features = ["rand_core"] }
lru = "0.12"
serde = { version = "1.0", features = ["derive"] }
rand = "0.8"
