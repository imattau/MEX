[package]
name = "rdma"
version = "0.1.0"
edition = "2021"

[features]
default = []
soft-rdma = []

[dependencies]
common = { path = "../common" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"

[build-dependencies]
cc = "1.0"
libloading = "0.8"
