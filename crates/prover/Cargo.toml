[package]
name = "prover"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
engine = { path = "../engine" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ark-ff = "0.4.0"
ark-relations = "0.4.0"
