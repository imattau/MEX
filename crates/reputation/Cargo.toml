[package]
name = "reputation"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
engine = { path = "../engine" }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
tracing = "0.1"
lru = "0.12"
metrics = "0.23"
sled = "0.34"
hex = "0.4"

[dev-dependencies]
rand = "0.8"
